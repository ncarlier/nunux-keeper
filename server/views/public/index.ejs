<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title><%= info.title %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=0"/>
  <meta name="description" content="<%= info.description %>" />
  <meta name="author" content="<%= info.author %>">
  <link rel="stylesheet" href="/stylesheets/public.css" />
  <!--[if lt IE 9]>
      <script src="lib/html5shiv/html5shiv.js"></script>
  <![endif]-->
  <link rel="apple-touch-icon-precomposed" sizes="152x152" href="icons/icon_152.png">
  <link rel="apple-touch-icon-precomposed" sizes="120x120" href="icons/icon_120.png">
  <link rel="apple-touch-icon-precomposed" href="icons/icon_57.png">
  <link rel="shortcut icon" href="icons/favicon.png">
</head>
<body>
  <header>
    <h1>Nunux Keeper public page</h1>
  </header>
  <section class="content">
    <% documents.forEach(function(doc){ %>
    <% include document %>
    <% }) %>
  </section>
  <footer>
  <div id="powered">
    <p>powered by Nunux <%= info.name %></p>
  </div>
  </footer>
<script>
var imgs = document.getElementsByTagName('img');
for (var i = 0; i < imgs.length; i++) {
  var img = imgs[i], src
      data = img.dataset;
  if (src = img.getAttribute('data-app-src')) {
    img.src = src;
  }
}
</script>
</body>
</html>
